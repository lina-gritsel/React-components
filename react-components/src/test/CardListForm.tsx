// import { render } from '@testing-library/react'

// import CardsList from '../pages/FormsPage/components/CardsList'

// describe('render CardsList in Form Page', () => {
//   it('render card', () => {
//     const { getByText } = render(<CardsList />)
//     const cardInfo = getByText(/Name/i)
//     expect(cardInfo).toBeInTheDocument()
//   })
// })
